.program logic_analyzer

.wrap_target
fast_entry:
    in pins, 8
.wrap

slow_entry:
    pull block
    mov y, osr
slow_loop:
    mov x, y
delay_loop:
    jmp x-- delay_loop
    in pins, 8
    jmp slow_loop